name: Env Secret Example
on: [push]
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Use secret as environment variable
        env:
          SECRET_VALUE: ${{ secrets.MY_SECRET_KEY }}
        run: echo "Secret value is $SECRET_VALUE"
